using System;
using System.Collections;
using System.Collections.Generic;
using Deepglint.XR;
using Deepglint.XR.Toolkit.Monitor;
using UnityEngine;

public class UITest : MonoBehaviour
{
    // private void Awake()
    // {
    //     SentryRuntimeConfiguration.EnvironmentName = DGXR.Config.Space.Name;
    // }

    // Start is called before the first frame update
    void Start()
    {
        Debug.LogError("Sentry-build-Start"); 
    }

    // Update is called once per frame
    void Update()
    {
        Debug.LogError("Sentry-build-Update");
    }
}
